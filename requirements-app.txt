# Demo-only dependencies (NOT needed for the scored run.sh pipeline).
# Install on top of requirements.txt to run the Streamlit app + LLM insights.
streamlit==1.41.1
plotly==5.24.1
python-dotenv==1.0.1
openai==1.59.6
